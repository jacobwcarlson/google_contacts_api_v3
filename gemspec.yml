name: google_contacts_api_v3
summary:
  A Rubyesque interface to the Google Contacts API, version 3.
description:
  A Rubyesque interface to the Google Contacts API, version 3.
  
license: MIT
authors: Jacob Carlson
email: the.real.jacobwcarlson@gmail.com
homepage: http://github.com/jacobwcarlson/google_contacts_api_v3

dependencies:
  json: >= 0.1
  oauth: >= 0.4.6

development_dependencies:
  rspec >= 1.0
